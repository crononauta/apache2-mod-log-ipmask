<IfModule log_ipmask_module>
	# Restrict logging of IPv4 addresses to the first 16 bits.
	LogDefaultIPv4Mask 16
	# Restrict logging of IPv6 addresses to the first 16 bits.
	LogDefaultIPv6Mask 16
</IfModule>
